# Build output
bin/
obj/

# NuGet restored packages (regenerate via `nuget restore`)
packages/

# wwwroot is generated by `npm run build` in client/ — don't commit build output
/wwwroot/

# Visual Studio / VS Code user-specific
*.user
*.suo
.vs/

# Logs
*.log

# AI assistant instructions (kept locally, not published)
CLAUDE.md
